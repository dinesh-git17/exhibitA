Review this entire session and update the persistent memory files.

Memory location: ~/.claude/projects/-Users-Dinesh-Desktop-exhibitA/memory/

Follow these rules exactly:

1. Read the current contents of MEMORY.md and any linked topic files before
   writing anything.
2. MEMORY.md -- update project identity, stack, conventions, constraints, or
   skills table only if something materially changed this session. Keep the
   file under 200 lines.
3. Decisions -- append new architectural or technical decisions made this
   session. Use the format: `- YYYY-MM-DD: <summary> -- <rationale>`. Use
   today's date. Never rewrite existing entries. If no decisions section
   exists in MEMORY.md or a topic file, create one.
4. Progress -- if tracking in-flight work, move completed items to a
   Completed section with today's date. Update In Flight to reflect current
   state. Write a concise Next Session note summarizing where to pick up.
5. Git activity -- record any branches pushed, PRs created, or merges
   completed this session.
6. Write all updates in place using the Edit tool. Do not recreate files.
7. Confirm each file written with its path.
